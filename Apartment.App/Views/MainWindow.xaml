<Window x:Class="Apartment.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        xmlns:gmaps="clr-namespace:GMap.NET.WindowsPresentation;assembly=GMap.NET.WindowsPresentation"
        xmlns:m="clr-namespace:GMap.NET.WindowsPresentation;assembly=GMap.NET.WindowsPresentation"
        Title="Test WPF OpenStreetMap" Height="500" Width="500">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="200" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Column="0" Text="{Binding Test}" VerticalAlignment="Center" HorizontalAlignment="Center" />
        <Grid Grid.Column="0" Grid.Row="1">
            <gmaps:GMapControl x:Name="Carte"
                               Loaded="Carte_Loaded" Margin="5" />
            <TextBlock Margin="10" Name="DiplayName"
                       TextWrapping="Wrap"
                       HorizontalAlignment="Center" />
        </Grid>
        <TextBox Grid.Column="0" Grid.Row="2" x:Name="PositionTextBox" IsReadOnly="true" VerticalAlignment="Center" HorizontalAlignment="Center" />

        <GridSplitter Grid.Row="0" Grid.RowSpan="3" Grid.Column="1" Width="2" HorizontalAlignment="Center" VerticalAlignment="Stretch" />

        <TextBlock Grid.Row="0" Grid.Column="2" Text="Список квартир" VerticalAlignment="Center" HorizontalAlignment="Center" />
        <ListBox Grid.Column="2" Grid.Row="1" x:Name="ApartmentsListBox" SelectionChanged="ApartmentsListBox_SelectionChanged" />
        <Button Grid.Column="2" Grid.Row="2" Content="Обновить список квартир" Click="Button_Click" />
    </Grid>
</Window>